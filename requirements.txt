# Python dependencies for the dice tray course project (client + data tooling).
# Firmware (code/firmware) runs on the Raspberry Pi Pico under MicroPython and
# has no pip dependencies.

flask==3.1.3
matplotlib==3.11.1
ncnn==1.0.20260526
numpy==2.5.1
opencv-python==5.0.0.93
pandas==3.0.5
Pillow==12.3.0
pyserial==3.5
PyYAML==6.0.3
scipy==1.18.0
torch==2.13.0
ultralytics==8.4.115

# Raspberry Pi only: picamera2 is not on PyPI; install it with:
#   sudo apt install python3-picamera2
# (used by code/client/data_collection/capture_die_image.py)
